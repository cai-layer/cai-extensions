# cai-extension
name: Uppercase
description: Convert text to UPPERCASE
author: cai-layer
version: "1.0"
tags: [text, formatting]
icon: textformat.size.larger
type: shell
shell: echo "{{result}}" | tr '[:lower:]' '[:upper:]'
